---
title: "test_wr"
output: html_notebook
---
```{r}

library(igraph)
# load raw network data ----
A <- utils::read.table(file = 'https://raw.githubusercontent.com/weisse-rose/Weisse-Rose/master/edgelist.csv',
                header = TRUE, sep = ',', quote = '', stringsAsFactors = FALSE)

# create network ----
wr <- graph_from_data_frame(A,directed = FALSE)

# load raw attribute data ----
attrs <- utils::read.table(file = 'https://raw.githubusercontent.com/weisse-rose/Weisse-Rose/master/nodelist.csv',
                    header = TRUE, sep = ',', quote = '', stringsAsFactors = FALSE)

plot(wr)

```

```{r Auswahl Edge-Attribute}
edge.attributes(wr)$relationship
edge.attributes(wr)$time
```

```{r Visualisierung nach Attribut time}
# setzt den Wert rot für alle Werte 1 = vor Flugblatt 1
E(wr)[E(wr)$time==1]$color<-"red"
E(wr)[E(wr)$time!=1]$color<-"black"
E(wr)[edge.width*10]
plot(wr)
wr

```

